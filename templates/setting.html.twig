<!DOCTYPE html>
<html lang="en">

<head>
    {% include 'inc/header.html.twig' %}
    <title>SQLView - Dashboard</title>
</head>

<body>

{% include 'inc/nav.html.twig' with {'page': 'setting'} %}

<div id="wrapper">

    <div class="container">

        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        Connections
                        <div class="pull-right">
                            <a href="/setting/connection/new" type="button" class="btn btn-xs btn-primary"
                               aria-label="Left Align">
                                <span class="glyphicon glyphicon-plus" aria-hidden="true"></span> New Connection
                            </a>
                        </div>
                    </div>
                    <div class="panel-body">
                        <div class="row">
                            <div class="col-lg-12">
                                <div class="table-responsive">
                                    <table class="table table-bordered table-hover table-striped">
                                        <thead>
                                        <tr>
                                            <th>Connection</th>
                                            <th>Server</th>
                                            <th>Health</th>
                                            <th width="100">Actions</th>
                                        </tr>
                                        </thead>
                                        <tbody>
                                        {% for connection in connections %}
                                            <tr>
                                                <td>{{ connection.cnn_name }}</td>
                                                <td>{{ connection.conn.cnn_host }}</td>
                                                <td>{{ connection.queries }}

                                                    {{ connection.connection_status }}

                                                </td>
                                                <td>
                                                    <a href="/database/edit/{{ connection.cnn_id }}"
                                                       class="btn btn-xs btn-primary">Edit</a>
                                                    <a href="/query/{{ connection.cnn_id }}"
                                                       class="btn btn-xs btn-primary">Query</a>
                                                </td>
                                            </tr>
                                        {% endfor %}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /.col-lg-4 -->
        </div>

    </div><!-- /.container -->

</div>
<!-- /#wrapper -->

{% include 'inc/footer.html.twig' %}
</body>

</html>
