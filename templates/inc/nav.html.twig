<nav class="navbar navbar-default navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar"
                    aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <a class="navbar-brand" href="#">SQLView</a>
        </div>
        <div id="navbar" class="collapse navbar-collapse">
            <ul class="nav navbar-nav">
                <li {% if page == "favorites" %}class="active"{% endif %}><a href="/favorites">Favorites</a></li>
                <li {% if page == "query" %}class="active"{% endif %}><a href="/query">Query</a></li>

                {% if user.usr_type == "admin" %}
                    <li {% if page == "connection" %}class="active"{% endif %}><a href="/connections">Connections</a>
                    </li>
                {% endif %}

                {% if user.usr_type == "admin" %}
                    <li {% if page == "user" %}class="active"{% endif %}><a href="/users">Users</a></li>
                {% endif %}

            </ul>
            <ul class="nav navbar-top-links navbar-right">
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                        <i class="fa fa-user fa-fw"></i> {{ user.usr_email }} <i class="fa fa-caret-down"></i>
                    </a>
                    <ul class="dropdown-menu dropdown-user">
                        <li><a href="/logout"><i class="fa fa-sign-out fa-fw"></i> Logout</a>
                        </li>
                    </ul>
                    <!-- /.dropdown-user -->
                </li>
                <!-- /.dropdown -->
            </ul>

            <ul class="nav navbar-top-links navbar-right">
                <li class="dropdown">
                    <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                        <i class="fa fa-plug fa-fw"></i> {{ active_connection.cnn_name }} {% if all_connections %}<i
                                class="fa fa-caret-down">{% endif %}</i>
                    </a>
                    {% if all_connections %}
                        <ul class="dropdown-menu dropdown-user">
                            {% for connection in all_connections %}
                                <li>
                                    <a href="/connection/change/{{ connection.cnn_id }}">{{ connection.cnn_name }}</a>
                                </li>
                            {% endfor %}
                        </ul>
                    {% endif %}
                    <!-- /.dropdown-user -->
                </li>
                <!-- /.dropdown -->
            </ul>

        </div><!--/.nav-collapse -->
    </div>
</nav>